/** 
  This is a mapping of routes to their respective paths.
  This is useful for when you want to change the path of a route.
  Instead of changing the path in every file that uses the route,
  you can change it here.
 */
export const routesMapping = {
  page: "/",
  signIn: "/sign-in",
  signUp: "/sign-up",
};
